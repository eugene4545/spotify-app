{
  "name": "spotify_playlist_downloader",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
